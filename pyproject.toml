[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]
ignore = ["E501"]  # line length — handled by formatter

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
